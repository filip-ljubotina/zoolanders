spring:
  datasource:
    password: ${DB_PASS:password}
    username: ${DB_USERNAME:username}
    url: "jdbc:postgres://zoolanders:EO0aLFJyva6jYEb2oUGsVDpkD0gxJToE@dpg-cl7s8aivokcc73aprek0-a.frankfurt-postgres.render.com/wildtrack_db"
    driverClassName: ${DB_DRIVER:org.postgresql.Driver}

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${EMAIL_USERNAME}
    password: ${EMAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
        connectiontimeout: 5000
        timeout: 3000
        writetimeout: 5000

